machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t aurora .

test:
  override:
    - docker run -d -p 8000:8000 --name aurora aurora;sleep 10
    - docker exec -i -t aurora python manage.py test
